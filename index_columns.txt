#Lines starting with # are comments
#Blank lines are skipped just like this:

#This file contains columns we are interested in indexing.
#Indexes are created for each table if the table columns match by string
#One per line. Simplest example:
uls_file_number
#...but sometimes the spelling & capitalization varies so we also add...
ULS_File_Number
uls_file_num

#Here are some batch filing numbers - 30 byte string
EBF_Number
ebf_number

#USI - 9 digit number
Unique_System_Identifier
unique_system_identifier
#Duplicates are ignored, so this is fine
unique_system_identifier
unique_system_identifier
unique_system_identifier

#If you need to do a composite index, you can specify it using commas. Ex:
state,city
zip_code,state,city

#Index names are generated as TABLENAME__COLUMNNAMEONE__COLUMNNAMETWO_...
#two _ are used as separator because STATUS_DATE might otherwise mean STATUS then DATE or STATUS_DATE
#And having read the FCC schema, they do not appear to use "__" in any column names
call_sign
Callsign
callsign

#more powah!
power_output
power_erp
minimum_power_output

#I wanna give you some good frequencies
frequency_assigned
frequency_upper_band
frequency_carrier
frequency
frequency_number
frequency_type_number
frequency_type_code
lower_frequency
upper_frequency
point_of_com_frequency
frequency_offset
frequency_channel_block
offset
